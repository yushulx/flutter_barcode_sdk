## 1.0.0

* Initial release.
* Provides the platform interface for `flutter_barcode_sdk`.
* Defines `BarcodeSdkPlatform` abstract class with a default
  `MethodChannelBarcodeSdk` implementation.
* Includes shared data models: `BarcodeResult`, `BarcodeFormat`,
  `ImagePixelFormat`, `ImageRotation`.
