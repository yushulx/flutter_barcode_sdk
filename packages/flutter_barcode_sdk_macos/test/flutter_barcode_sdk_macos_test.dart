import 'package:flutter_test/flutter_test.dart';

void main() {
  test('flutter_barcode_sdk_macos package compiles', () {
    // Smoke test: verifies the package structure is valid.
    // Native platform code is verified at build time on macOS.
    expect(true, isTrue);
  });
}